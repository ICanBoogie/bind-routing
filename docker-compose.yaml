---
services:
  app84:
    build:
      context: .
      args:
        PHP_VERSION: "8.4"
    environment:
      PHP_IDE_CONFIG: 'serverName=icanboogie-<name>'
    volumes:
      - .:/app:delegated
      - ~/.composer:/root/.composer:delegated
    working_dir: /app
